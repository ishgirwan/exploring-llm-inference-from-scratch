# Copy to .env and fill in. .env is gitignored; this file documents what's needed.

# Anthropic API key for the agent loop (agent/loop.py)
ANTHROPIC_API_KEY=

# Modal auth is NOT kept here — `modal token new` stores it in ~/.modal.toml
